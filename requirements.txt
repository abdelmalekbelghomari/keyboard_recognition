# Python Dependencies
librosa==0.8.0
tensorflow==2.5.0
numpy==1.19.5
pandas==1.2.4
scipy==1.6.3
matplotlib==3.4.2

# System Dependencies
ffmpeg==4.3.1  # Replace with the version compatible with your system.

# Note:
# Ensure you have a local PHP server running for the development environment.
# The web interface requires PHP until it is fully deployed on the internet.

